server:
  port: 9000

spring:
  cloud:
    stream:
      bindings:
        input:
          destination: learning-spring-boot
          group: comments-service
          content-type: application/json
        output:
          destination: learning-spring-boot
          group: comments-chat
